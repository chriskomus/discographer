<p style="color: green"><%= notice %></p>

<h1>Releases</h1>

<div id="releases">
  <% @releases.each do |release| %>
    <%= render release %>
    <p>
      <%= link_to "Show this release", release %>
    </p>
  <% end %>
</div>

<%= link_to "New release", new_release_path %>
