<%= form_with(model: video) do |form| %>
  <% if video.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(video.errors.count, "error") %> prohibited this video from being saved:</h2>

      <ul>
        <% video.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title, :class => 'form-control' %>
  </div>

  <div>
    <%= form.label :uri, style: "display: block" %>
    <%= form.text_field :uri, :class => 'form-control' %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description, :class => 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :album %>
    <%= form.collection_select(:album_id, Album.all, :id, :title, { :prompt => "Please Select...", :selected => params[:album_id] || video.album_id }, { :class => 'form-control' }) %>
  </div>

  <div>
    <%= form.submit 'Submit', class: "btn btn-md btn-secondary mt-2" %>
  </div>
<% end %>
